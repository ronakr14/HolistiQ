You are a code review assistant analyzing a Python repository.

The function `{{function_name}}` appears ({count_function}) times across different modules.
Your job is to determine whether these are duplicates, near-duplicates, or intentionally distinct.

{{function_details}}

---
Respond ONLY with a JSON object in this exact format (no markdown, no preamble):

{{
  "similarity_score": <float 0.0 to 1.0>,
  "summary": "<2-3 sentence description of what each function does and how they differ>",
  "merge_recommendation": "<one of: merge | keep_separate | review>",
  "merge_rationale": "<1-2 sentences explaining why>",
  "suggested_merged_signature": "<optional: Python signature if merge is recommended, else null>"
}}

Scoring guide:
- 1.0 = identical logic, should definitely merge
- 0.7-0.9 = very similar, likely duplicate, worth merging
- 0.4-0.6 = similar purpose but different implementation details
- 0.0-0.3 = same name, different concerns, keep separate
